export const apiUri = {
  baseUri: String(import.meta.env.VITE_BASE_URI),
  songUri: String(import.meta.env.VITE_SONG_URI),
  albumUri: String(import.meta.env.VITE_ALBUM_URI),
  usersUri: String(import.meta.env.VITE_USERS_URI),
};
